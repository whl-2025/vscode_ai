# Max Tokens 测试指南

## 📋 如何设置 Max Tokens

### 方法1：通过配置面板
1. 在聊天界面点击右侧的 ⚙️ **配置** 按钮
2. 或者在命令面板输入 `Ccdc: Open Configuration`
3. 找到 **Max Tokens** 输入框
4. 设置为 `5`
5. 点击 **Save Configuration** 保存

### 方法2：通过 VS Code 设置
1. 按 `Ctrl+,` (Windows/Linux) 或 `Cmd+,` (macOS) 打开设置
2. 搜索 `ccdcCodeGen.maxTokens`
3. 将值改为 `5`

## ⚠️ 重要说明：Max Tokens 的含义

### Max Tokens 只限制 AI 的输出（回复），不包括输入（问题）

**关键理解：**
- ✅ **Max Tokens = 5**：只限制 AI **回复**的 token 数量（最多 5 个 tokens）
- ❌ **不包括**：用户发送的问题、系统提示词等输入内容

**举例说明：**
```
输入（不算 Max Tokens）：
- 系统提示词：假设 200 tokens
- 用户问题："请详细解释一下什么是 JavaScript"：假设 10 tokens
- 总计输入：210 tokens

输出（受 Max Tokens = 5 限制）：
- AI 回复：最多 5 个 tokens（例如："JavaScript 是一种"）

总 Token 使用：210（输入）+ 5（输出）= 215 tokens
```

**所以：**
- 你可以发送任意长度的问题（只要不超过模型的上下文窗口限制）
- 但 AI 的回复会被限制在 Max Tokens 指定的数量内

## 🧪 测试问题建议（Max Tokens = 5）

当 Max Tokens 设置为 5 时，AI **回复**只能生成约 5 个 tokens（约 3-10 个中文字符或 5-10 个英文字符）。

**注意**：问题的长度不影响 Max Tokens，你甚至可以发送很长的问题，但回复仍然会被限制在 5 个 tokens。

### ✅ 推荐测试问题（能明显看到限制效果）

#### 1. 简单问题测试
```
问题: "你好"
预期: 回复应该在 5 个 tokens 内，可能只回复 "你好" 或 "您好" 就结束了
```

#### 2. 需要详细回答的问题（会被截断）
```
问题: "请详细解释一下什么是 JavaScript"
预期: 回复会非常简短，可能只有几个字就被截断，例如：
- "JavaScript 是"
- "JavaScript 是一种"
- 或者完全空白（如果连基本回答都不够）
```

#### 3. 代码生成测试
```
问题: "生成一个完整的 Python Hello World 程序"
预期: 回复可能只有：
- "def hello():"
- "print('"
- 或者只有注释的开始
```

#### 4. 列表问题测试
```
问题: "列出 5 种编程语言"
预期: 可能只回复：
- "1. Python"
- "1. Python, 2"
- 或者只有 "1."
```

#### 5. 要求计数的问题
```
问题: "从 1 数到 10"
预期: 可能只回复：
- "1, 2, 3"
- "1, 2, 3, 4"
- 或者更少
```

#### 6. 简单指令测试
```
问题: "说一句话介绍你自己"
预期: 可能只回复：
- "我是"
- "我是 AI"
- "我是 AI 助"
```

#### 7. 长句测试
```
问题: "请用一段话详细描述人工智能的发展历史"
预期: 回复会立即被截断，可能只有：
- "人工智能发展"
- "人工智能发展历史"
- 或者一个词都没有
```

## 🔍 如何验证 Max Tokens 是否生效

### 1. 检查回复长度
- **正常情况**（Max Tokens = 1024）：回复通常有几十到几百个字符
- **限制情况**（Max Tokens = 5）：回复非常短，通常少于 10 个字符

### 2. 查看控制台日志（如果启用 debug 日志）
- 打开开发者工具：`Help` → `Toggle Developer Tools`
- 查看 Console 标签
- 应该能看到请求中的 `max_tokens: 5`

### 3. 对比测试
```
测试步骤：
1. 设置 Max Tokens = 5，发送问题："解释一下什么是编程"
2. 记录回复长度（例如：5 个字符）
3. 设置 Max Tokens = 100，发送同样的问题
4. 对比回复长度（应该明显更长，几十到上百个字符）
```

## 📊 预期行为

### Max Tokens = 5 时的典型行为

| 问题类型 | 预期回复长度 | 示例 |
|---------|------------|------|
| 简单问候 | 2-5 个字符 | "你好"、"您好" |
| 详细解释问题 | 5-10 个字符（被截断） | "JavaScript 是一种" |
| 代码生成 | 5-10 个字符（不完整） | "def hello" |
| 列表问题 | 5-10 个字符（只列出 1-2 项） | "1. Python" |
| 需要长回答的问题 | 可能完全空白或极短 | "" 或 "我" |

### 注意事项
- **中文字符 vs 英文 tokens**：1 个中文字符可能等于 1-2 个 tokens
- **token 计算**：实际的 token 计算取决于模型的 tokenizer
- **流式输出**：在流式输出中，回复会在达到限制时立即停止

## 🎯 最佳测试问题（推荐）

### 问题 1：简单测试（推荐）
```
问题: "你好，请介绍一下你自己"
预期: 回复非常短，可能只有 "你好" 或 "我是" 就结束了
```

### 问题 2：详细回答测试（推荐）
```
问题: "请详细说明如何使用 Python 读取文件"
预期: 回复可能只有 "使用 open" 或 "使用 open()" 就结束了
```

### 问题 3：代码测试（推荐）
```
问题: "写一个完整的函数来计算两个数的和"
预期: 回复可能只有 "def add" 或 "def add(" 就结束了
```

### 问题 4：列表测试（推荐）
```
问题: "列出前端开发的三个核心技术"
预期: 回复可能只有 "1. HTML" 或 "1. HTML, 2." 就结束了
```

## 🔧 调试技巧

### 1. 启用 Debug 日志
1. 在配置面板设置 `Log Level` 为 `debug`
2. 查看输出面板的日志信息

### 2. 检查网络请求
1. 打开开发者工具 (`F12` 或 `Ctrl+Shift+I`)
2. 切换到 `Network` 标签
3. 发送消息
4. 找到 `chat/completions` 请求
5. 查看 `Request Payload` 中的 `max_tokens` 值

### 3. 对比测试
- 先测试 `maxTokens = 5`（回复很短）
- 再测试 `maxTokens = 100`（回复明显更长）
- 再测试 `maxTokens = 1000`（完整回复）

## ⚠️ 常见问题

### Q: 为什么设置了 5 但回复还是有几十个字符？
**A**: 可能是：
- 配置没有保存成功（检查配置面板的值）
- 插件没有重新加载配置（尝试重启 VS Code 或重新打开聊天面板）
- 某些模型可能有最小 token 输出限制

### Q: 回复完全空白怎么办？
**A**: 
- 检查 `maxTokens` 是否确实为 5
- 查看控制台是否有错误信息
- 尝试设置为更大的值（如 10 或 20）测试

### Q: 如何确认配置已生效？
**A**:
- 在配置面板查看当前值
- 查看开发者工具的 Network 请求中的 `max_tokens` 字段
- 对比不同 `maxTokens` 值的回复长度

## 📝 测试清单

- [ ] 设置 Max Tokens = 5
- [ ] 发送问题："你好"（预期：简短回复）
- [ ] 发送问题："详细解释一下什么是编程"（预期：回复被截断）
- [ ] 发送问题："生成一个完整的 Hello World 程序"（预期：代码不完整）
- [ ] 对比测试：Max Tokens = 5 vs Max Tokens = 100
- [ ] 检查网络请求中的 `max_tokens` 值
- [ ] 验证回复确实在 5 tokens 左右结束
- [ ] 测试完成后恢复 Max Tokens 为默认值（1024）

---

**提示**: 测试完成后记得将 Max Tokens 恢复为正常值（如 1024 或 2048），否则后续使用会受到影响！

